# this is the port on which our random-quote-service will run
server.port=3344

# allow for RefreshScope
management.endpoints.web.exposure.include=*

randomQuoteServiceName=Priti-Solanki-random-quote-service
serviceProtocol=http://
servicePath=/quote
